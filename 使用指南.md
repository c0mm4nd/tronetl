1. **抽取交易数据 (外部交易)**
您可以使用 `export_blocks_and_transactions` 命令。外部交易会保存在您通过 `-transactions-output` 参数指定的文件中。
    
    ```bash
    tronetl export_blocks_and_transactions \
        --start-block <起始区块> \
        --end-block <结束区块> \
        --provider-uri <节点地址> \
        --transactions-output transactions.csv
    ```
    
2. **抽取内部交易**
您可以使用 `export_token_transfers` 命令。内部交易数据会保存在您通过 `-internal-tx-output` 参数指定的文件中。
    
    ```bash
    tronetl export_token_transfers \
        --start-block <起始区块> \
        --end-block <结束区块> \
        --provider-uri <节点地址> \
        --internal-tx-output internal_transactions.csv
    ```
    
3. **抽取合约数据 (识别合约地址)**
您可以使用 `export_address_details` 命令来获取合约的详细信息。

首先，您需要一个包含地址列表的CSV文件（例如，`addresses.csv`），每行一个地址。然后，运行以下命令：

```bash
tronetl export_address_details \
    --addrs-source addresses.csv \
    --contracts-output contracts.csv \
    --provider-uri <节点地址>
```

这将会生成一个 `contracts.csv` 文件，其中包含有关这些地址是否是合约以及合约的详细信息。

4. **抽取 代币数据 (名称、发行量、精度)**
您可以使用 `export_address_details` 命令来获取代币的元数据，如名称、总供应量和精度。

首先，您需要一个包含代币合约地址的CSV文件（例如，`token_addresses.csv`），每行一个地址。然后，运行以下命令：

```bash
tronetl export_address_details \
    --addrs-source token_addresses.csv \
    --tokens-output tokens.csv \
    --provider-uri <节点地址>
```

这将会生成一个 `tokens.csv` 文件，其中包含代币的详细信息。

5. **抽取代币交易 (TRC10, TRC20, TRC721, TRC1155)**
    - **TRC10**: 使用 `export_blocks_and_transactions` 命令，并使用 `-trc10-output` 参数指定输出文件。
        
        ```bash
        tronetl export_blocks_and_transactions \
            --start-block <起始区块> \
            --end-block <结束区块> \
            --provider-uri <节点地址> \
            --trc10-output trc10_transfers.csv
        ```
        
    - **TRC20, TRC721, TRC1155**: 使用 `export_token_transfers` 命令，并使用 `-transfers-output` 参数。导出的文件中包含了这些类型代币的转账事件。
        
        ```bash
        tronetl export_token_transfers \
            --start-block <起始区块> \
            --end-block <结束区块> \
            --provider-uri <节点地址> \
            --transfers-output token_transfers.csv
        ```

